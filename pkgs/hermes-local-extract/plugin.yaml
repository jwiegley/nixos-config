name: web-local-extract
version: 1.0.0
description: "Fully local URL→Markdown extraction via trafilatura. No API key, no external service — pairs with SearXNG (search-only) to complete the web capability without disclosing URLs to a third party."
author: vulcan
kind: backend
provides_web_providers:
  - local
