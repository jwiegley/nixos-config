# Home Assistant backup data (contains sensitive OAuth tokens and device pairings)
home-assistant-backup/
home-assistant-backup_*.tar.gz

# SOPS age keys (already encrypted, but keep private)
*.age
/secrets.yaml
*.bak

# Temporary files
*.tmp
*.swp
*~

# Build outputs
result
result-*
