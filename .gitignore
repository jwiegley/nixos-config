/firmware

# SOPS age keys (keep private - these decrypt the secrets)
*.age
*.bak

# Note: secrets.yaml IS encrypted by SOPS and MUST be tracked in git
# for Nix flakes to build. Only the .age private keys must stay private.

# Temporary files
*.tmp
*.swp
*~

# Build outputs
result
result-*
