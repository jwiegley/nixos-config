# Home Assistant backup data (contains sensitive OAuth tokens and device pairings)
home-assistant-backup/
home-assistant-backup_*.tar.gz

# SOPS age keys (keep private - these decrypt the secrets)
*.age
/secrets.yaml
*.bak

# Note: secrets.yaml is encrypted by SOPS and SHOULD be tracked in git
# Only the .age private keys need to be kept out of version control

# Temporary files
*.tmp
*.swp
*~

# Build outputs
result
result-*
